on:
  push:
    branches: [ master ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - run: |
          git clone https://github.com/d-z4/spark.git
          cd spark
          chmod +x gradlew
          ./gradlew publishToMavenLocal
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25
      - run: |
          cd nodes
          chmod +x gradlew
          ./gradlew clean build
